<ContentDialog
    x:Class="IconPackBuilder.Views.PreviewIconPackDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IconPackBuilder.Views"
    xmlns:vm="using:IconPackBuilder.ViewModels"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:suxc="using:Singulink.UI.Xaml.Converters">

    <Grid Width="500" Height="600" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" CornerRadius="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Preview Icon Pack" FontSize="20" FontWeight="Bold" Margin="16" />

        <ListView Grid.Row="1" ItemsSource="{x:Bind Model.Icons, Mode=OneWay}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PreviewIconItem">
                    <StackPanel Orientation="Horizontal" Spacing="12" Padding="8">
                        <FontIcon Glyph="{x:Bind Glyph}" FontSize="24" FontFamily="{utu:AncestorBinding AncestorType=ContentDialog, Path=IconFontFamily}" />
                        <FontIcon Glyph="{x:Bind RtlGlyph}" FontSize="24" FontFamily="{utu:AncestorBinding AncestorType=ContentDialog, Path=IconFontFamily}"
                                  Visibility="{x:Bind suxc:Visible.IfNotNull(RtlGlyph)}" />
                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="2" Content="Close" Command="{x:Bind Model.CloseCommand}" HorizontalAlignment="Right" Margin="16" />
    </Grid>
</ContentDialog>
